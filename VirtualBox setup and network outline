While working on this project I created two seperate networks to simulate a LAN and WAN enviroment. I put the Ubuntu VM, Windows server VM, and metasploit vm on the same LAN network though a host-only adapter to act as the victims.
I kept the Kali linux VM on the NAT adapter to smilate a WAN/internet enviroment to act as an attacker. I included the Metasploit VM to smillate a vulnerable enviroment that I could exploit with my attacking machine. I set up pfsense as my firewall and configured the Ubutntu, Windows server, and metasplotable vm to the LAN on the network while putting . I kept the ip addresses for my LAN systems to stay static so I could identify them 
