I assigned the interfaces on Pfsense for the Virtual machines.  The Kali machine is set to through the network interface em0 on the WAN. While the Ubuntu, Windows server, Metasploitable virtual machines were kept in the LAN. I set the interfaces IP addresses in the WAN and LAN on pfsense. I kept the LAN static with IP address 192.168.1.1 and the WAN dynamic with IP address 10.0.2.15.![[Pasted image 20260619224014.png]]